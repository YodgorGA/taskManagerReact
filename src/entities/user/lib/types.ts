export interface UserInfo{
    username:string | null
    about:string | null,
    id:string | null,
    photoUrl:string | null

}

export interface authInfo{
    login:string,
    password:string
}

export {}